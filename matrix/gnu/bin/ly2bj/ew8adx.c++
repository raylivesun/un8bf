#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

void ew8adx(int mode, long position, char* it, float m)
{
    mode = 512;
    position = mode;
    it[512] = m;
    return ;

    if (mode != position) 
    {
        static_assert("it[512] == m");
    } else {
        return ;
    }

  obstack;
}

